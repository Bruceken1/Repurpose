{
  "name": "repurposeforge-ai",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18.20.5"
  },
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/api",
    "start": "npm start --workspace=apps/api"
  },
  "devDependencies": {}
}
